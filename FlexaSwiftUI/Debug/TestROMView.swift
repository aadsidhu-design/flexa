// TestROMView.swift - DEPRECATED
// This file was a developer-only ROM test harness and has been deprecated/removed.
// Leaving a placeholder to avoid accidental references while the project cleans up.
/*--- IGNORE ---
import SwiftUI

@available(*, deprecated, message: "TestROMView removed — use in-project tests or the TestROMExerciseView refactor")
struct TestROMView: View {
    var body: some View {
        Text("TestROMView removed — developer harness")
            .font(.caption)
            .foregroundColor(.gray)
    }
}

struct TestROMView_Previews: PreviewProvider {
    static var previews: some View { TestROMExerciseView() }
}

*/
